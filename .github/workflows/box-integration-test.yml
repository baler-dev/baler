name: "Box Integration Test"
description: "To test if the modules installed by `{baler}` can be attached by `{box}` R package"

on:
    push:
        branches: [main]
    pull_request:
    workflow_dispatch:

jobs:
    box-integration:
        runs-on: ${{ matrix.os }}
        timeout-minutes: 20

        strategy:
            fail-fast: false
            matrix:
                os: [ubuntu-latest, macos-latest, windows-latest]

        steps:
            - uses: actions/checkout@v4

            - name: "Install rust toolchain"
              run: |
                  rustup toolchain install
                  rustup show

            - name: "Build baler"
              run: cargo build --release --locked -p baler-cli

            - name: "Set up R"
              uses: r-lib/actions/setup-r@v2
              with:
                  r-version: "4.5.3"
                  use-public-rspm: true

            - name: "Install the patched {box} fork"
              run: |
                  Rscript -e 'install.packages("pak")'
                  Rscript -e 'pak::pak("joshuamarie/box@feature/baler-module-support")'

            - name: "Fetch fpurrr and statmodule"
              run: | 
                  git clone --depth 1 https://github.com/joshuamarie/fpurrr.git fpurrr-src
                  git clone --depth 1 https://github.com/joshuamarie/stat-box-module.git statmodule-src

            - name: "Install a real module via baler"
              shell: bash
              run: |
                  if [ "${{ runner.os }}" = "Windows" ]; then
                      ./target/release/baler.exe install ./fpurrr-src --install-deps
                      ./target/release/baler.exe install ./statmodule-src --install-deps
                  else
                      ./target/release/baler install ./fpurrr-src --install-deps
                      ./target/release/baler install ./statmodule-src --install-deps
                  fi

            - name: "Verify box::use() resolves the installed module"
              run: Rscript scripts/verify-box-use.R
